import React from 'react'

const TrendsLayout = ({ 
  children,
}: Readonly<{ 
  children: React.ReactNode; 
}>) => {
  return (
    <div>
      TrendsLayout
      {children}
    </div>
  )
}

export default TrendsLayout
